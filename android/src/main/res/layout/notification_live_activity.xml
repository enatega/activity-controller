<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/compactRoot"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="12dp"
    android:background="@drawable/bg_notification">
    <ImageView android:layout_width="86dp" android:layout_height="38dp" android:contentDescription="Yalla" android:scaleType="centerCrop" android:src="@drawable/yalla_logo" />
    <TextView android:id="@+id/tvHeadline" android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:ellipsize="end" android:maxLines="1" android:paddingStart="8dp" android:text="Almost here!" android:textColor="#FFFFFF" android:textSize="14sp" android:textStyle="bold" />
    <TextView android:id="@+id/tvEta" android:layout_width="wrap_content" android:layout_height="wrap_content" android:background="@drawable/bg_eta" android:gravity="center" android:minWidth="54dp" android:paddingHorizontal="8dp" android:paddingVertical="7dp" android:text="8 min" android:textColor="#FFCA0D" android:textSize="14sp" android:textStyle="bold" />
</LinearLayout>
